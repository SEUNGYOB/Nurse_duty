{
  "buildCommand": "python3 -c \"import re,time,pathlib; p=pathlib.Path('sw.js'); p.write_text(re.sub(r'nurse-shift-v[^\\\"]+', 'nurse-shift-v'+str(int(time.time())), p.read_text()))\"",
  "outputDirectory": ".",
  "functions": {
    "api/parse-duty.py": {
      "maxDuration": 120,
      "memory": 1024
    },
    "api/share.py": {
      "maxDuration": 10,
      "memory": 256
    }
  }
}
